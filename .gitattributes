# Keep POSIX shell scripts LF-only so shebangs survive Windows checkouts
# (core.autocrlf=true would otherwise inject CRLF and break `#!/usr/bin/env bash`).
*.sh text eol=lf
*.ps1 text eol=crlf
*.cmd text eol=crlf
Dockerfile text eol=lf
.replit text eol=lf
replit.nix text eol=lf
